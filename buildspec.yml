version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto21
    commands:
      - echo "Installing dependencies..."

  build:
    commands:
      - echo "Building Spring Boot application..."
      - mvn clean package -DskipTests

artifacts:
  files:
    - target/*.jar
  discard-paths: yes